apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-data
  namespace: media
  labels:
    recurring-job.longhorn.io/backup-mealie-weekly: enabled
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: replace-me
  resources:
    requests:
      storage: 2Gi
